# FDE Day-One Brief

Auto-generated answers to the five questions every new FDE needs answered. Evidence from static analysis and data lineage (file paths and graph traversal).

---

## 1. What is the primary data ingestion path?

Primary ingestion path not inferred (run with OPENROUTER_API_KEY for LLM synthesis).

---

## 2. What are the 3-5 most critical output datasets/endpoints?

Critical outputs not inferred (run with OPENROUTER_API_KEY).

---

## 3. What is the blast radius if the most critical module fails?

Blast radius not inferred (run with OPENROUTER_API_KEY).

---

## 4. Where is the business logic concentrated vs. distributed?

Business logic distribution not inferred (run with OPENROUTER_API_KEY).

---

## 5. What has changed most frequently in the last 90 days (git velocity map)?

Velocity map from survey_analytics high_velocity_files (run with API key for full synthesis).

---
